<style>
  .sign-in-list{
    display:none;
  }
  @media screen and (max-width: 761px) {
    .sign-in-list{
      background: rgb(0,0,0,.7);
      color: #e0004d;
      position: fixed;
      bottom: 0;
      left: 0;
      z-index: 5;
      width: 100%;
      display: flex;
      height: 45px;
      line-height: 45px;
      padding: 0px 20px;
      justify-content: space-between;
  }
    .sign-in-text{
      font-family: var(--heading-font-family);
      font-weight: 500;
      font-size: 16px;
      color:#fff;
    }
    .sign-in-btn{
      background:#F6DDD8;
      height: 30px;
      padding: 5px 15px;
      line-height: 17px;
      margin-top: 7px;
      border-radius: 20px;
    }
  {% if customer.name == blank %}
    div.lucky-coupon-bar{
      bottom:0px !important;
    }
    #dummy-chat-button-iframe{
      bottom:100px !important;
    }
  {% endif %}
  }
</style>
<section class="shopify-section shave-section shopify-section--video">
  {% if customer.name == blank %}
    <div class="sign-in-list">
      <div class="sign-in-text">Sign in for Better Experience</div>
      <div class="sign-in-btn">
        <a href="/account">Sign in</a>
      </div>
    </div>
  {% endif %}
</section>
<script type="text/javascript">
  // $(document).ready(function () {
  //   let cookie = getCookie('lkda_spinthewheel57296CountDownShowed');
  //   if(cookie){
  //     let cookie13=new Date(Number(cookie))
  //     cookie13.setTime(cookie13.getTime() + (30 * 60 * 1000));
  //     let jia_datetime = cookie13.getTime()
  //      var d_datetime = new Date().getTime();
  //     if(d_datetime>jia_datetime){
  //       //$('.sign-in-list').show()
  //     }else{
  //       $('.sign-in-list').hide()
  //     }
  //   }else{
  //      //$('.sign-in-list').show()
  //   }
  //   function getCookie(cookieName) {
  //     const strCookie = document.cookie
  //     const cookieList = strCookie.split(';')

  //     for(let i = 0; i < cookieList.length; i++) {
  //       const arr = cookieList[i].split('=')
  //       if (cookieName === arr[0].trim()) {
  //         return arr[1]
  //       }
  //     }
  //     return ''
  //   }
  // })
</script>
{% schema %}
{
  "name": "sign-in-shopfiy",
  "blocks": [],
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Shave title"
    }
  ],
  "presets": [
    {
      "name": "sign-in-shopfiy"
    }
  ]
}
{% endschema %}
